<script type="text/javascript">
    $(document).ready(function() {
        $('.multiselect').multiselect();
    });
</script>

<div class="container extra-pad">
<p>Perfect! Now let’s get to the specifics. For each category you selected in step 1, please provide any and all details so your friends and family can pitch in on exactly what you need! Let’s figure out what you need when it comes to meals, chores, and pet care</p>
<hr>
<div class="row">
  <%= form_for @event do |f|%>
    <% @event.categories.each do |category| %>
    <div class="col-md-3">
      <%= category.name %><br><br>
      <%= f.select :task_ids, options_from_collection_for_select(category.tasks.map { |task| task }, :id, :name, category.name), {}, {class: 'multiselect', multiple: true} %>
    </div>
    <% end %>
  </div>
  <br>
  <div class="row text-center"><button type="submit" class="btn btn-default submit-button">Add selected tasks</button></p></div>
<% end %>
</div>

<div class="container">
<% if @event.tasks != nil %>

Last step: Let’s put these activities and requests on the calendar! Drag and drop each onto the hour, day, week, or month you need the support.
Last step: Let’s put these activities and requests on the calendar! Tell us the date and time you want these help with each of these tasks.
<% @event.tasks.each do |task| %>
<p><%= task.name %></p>
<% end %>

<%#= month_calendar do |date| %>
  <%#= date %>
<%# end %>
<div id="calendar"></div>
<% end %>

</div>
