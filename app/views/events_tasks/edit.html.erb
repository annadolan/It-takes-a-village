<div class="container extra-pad">
  <div class ="row">
    <div class = "text-center">
      <div class="col-sm-3">
      </div>
      <div class="col-sm-6">

<h3>Last step: Let’s put these activities and requests on the calendar! Tell us the date and time you want these help with each of these tasks.</h3>

<div class="form-group">
<%= form_for :events_tasks, url: edit_event_patch_path, method: :patch, class: "form-group" do |f| %>
  <% @events_tasks.each do |event_task| %>
    <%= fields_for "events_tasks[]", event_task do |u| %>
    <p>Task name: <%= find_task_name(event_task)  %></p>
    <p><%= u.label :date %>:<%= u.text_field :date %></p>
  <% end %>
<% end %>
<%= f.submit "Update these dates" %>
<% end %>
</div>

</div>
</div>
</div>
