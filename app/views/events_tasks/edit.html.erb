<div class="container extra-pad">
  <div class ="row">
    <div class = "text-center">
      <div class="col-sm-3">
      </div>
      <div class="col-sm-6">
        <div class="panel-body less-padding">
        </div>
        <p>Last step: Let’s put these activities and requests on the calendar! Tell us the date and time you want these help with each of these tasks.</p>

        <div class="form-group">
          <%= form_for :events_tasks, url: edit_event_patch_path, method: :patch, class: "form-group" do |f| %>
          <div class="form-group">
            <% @events_tasks.each do |event_task| %>
            <%= fields_for "events_tasks[]", event_task do |u| %>
            <div class="form-group">
              <p><strong>Task name: <%= find_task_name(event_task) %></p></strong>
            </div>
            <div class="form-group">
              <p><%= u.label :date %>: <%= u.text_field :date %></p>
            </div>
            <% end %>
            <% end %>
            <button type="submit" class="btn btn-default submit-button">Update these dates</button></p>
            <% end %>
          </div>
        </div>
      </div>
    </div>
  </div>
  <div class="col-sm-3">
  </div>
</div>
